# Python Barcode

```bash
# Rodar a aplicação em python no modo DEBUG
python run.py

# Ativar as variáveis ambientes do Python
./.venv/Scripts/activate

# Gerar as dependências dos pacotes da aplicação (Windows)
./.venv/Scripts/pip freeze > requirements.txt
```
